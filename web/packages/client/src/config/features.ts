/**
 * App-wide feature toggles (single source of truth for web + backend).
 * Edit these values and rebuild/redeploy both apps.
 */
export const ENABLE_PHONE_FEATURES = false;

export const ENABLE_GENERAL_SMS = false;
