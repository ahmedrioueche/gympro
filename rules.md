create page, the ui must match exatly the other pages using PageHeader and colors of index.css and components in global components folder and all text must be translated and added to en.json without adding fallback text so the missing translations are apparent, split the page into components in ./components and hooks for logic in ./hooks and queries for data, use getMessage and ShowStatusToast for handling messages, create the needed api endpoints and types in client package (only neccessary, no duplication, no creating a mess) and return ApiResponse for all , create the module, schema (if needed), controller and service return apiResponse in controller with error codes, add the error codes to error.ts in client/types and in web/utils/statusMessage.ts
